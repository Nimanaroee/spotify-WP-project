import type { ReactNode } from 'react'

/**
 * MainLayout — Sidebar + Navbar + Outlet + PlayerBar placeholder
 * Spec reference: §2.2, §2.9
 */
export default function MainLayout({ children: _children }: { children: ReactNode }) {
  return <div className="p-6">TODO: MainLayout</div>
}
